services:
  portfolio:
    image: nginx:alpine
    container_name: portfolio-kishmish
    restart: unless-stopped

    volumes:
      - ./app:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/nginx.conf:ro

    networks:
      - inside-net

networks:
  inside-net:
    external: true